import { CoApplicationForm } from './co-application-form.model';

describe('CoApplicationForm', () => {
  it('should create an instance', () => {
    expect(new CoApplicationForm()).toBeTruthy();
  });
});
